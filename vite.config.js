// vite.config.js
import { defineConfig } from 'vite';

export default defineConfig({
  server: {
    hmr: true, // Você pode definir como false para desativar o HMR
  },
  // Adicione outras configurações se necessário
});